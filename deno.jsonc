{
    "tasks": {
        "start": "ENV=production deno run -A  --reload=http://localhost:9000,http://localhost:9002 --no-lock --unstable --unstable-kv --unstable-sloppy-imports main.ts",
        "dev": "ENV=development WATCH=true PORT=9002 deno run -A --reload=http://localhost:9000,http://localhost:9002 --no-lock --unstable --unstable-kv --unstable-sloppy-imports main.ts",
        "file-loader": "deno run -A -r --no-lock --unstable file-loader.ts",
        "api": "deno run -A -r --no-lock --unstable file-loader.ts"
    },
    "imports": {
        "react": "npm:react@^18.3.1",
        "react-dom": "npm:react-dom@^18.3.1"
      }
}