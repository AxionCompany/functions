{
    "tasks": {
        "file-loader": "PORT=8000 HANDLER_TYPE=file-loader deno run -A -r --no-lock --unstable main.ts",
        "api": "PORT=8001 HANDLER_TYPE=rpc deno run -A -r --no-lock --unstable main.ts",
        "main": "deno run -A -r --no-lock --unstable main.ts",
        "dev": "PORT=9002 deno run -A -r --no-lock --unstable main.ts"
    }
}